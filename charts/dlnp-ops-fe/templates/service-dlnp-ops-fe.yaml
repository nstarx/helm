apiVersion: v1
kind: Service
metadata:
  name: dlnp-ops-fe
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  selector:
    app: dlnp-ops-fe
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
